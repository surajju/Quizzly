fix: preserve chat messages across screen transitions

Move chat state from per-component useChat hook into a ChatContext
provider at the app root, so messages survive route changes between
lobby, question, reveal, and end screens.
